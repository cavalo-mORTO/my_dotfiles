#!/bin/sh

# Terminate already running bar instances
pid=$(ps -C polybar -o pid=)

[ ! -z "$pid" ] && kill -9 ${pid[@]}

# Launch Polybar, using default config location ~/.config/polybar/config
polybar main &

echo "Polybar launched..."
